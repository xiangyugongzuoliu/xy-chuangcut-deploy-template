# ========================================
# 创剪视频工作流 - 环境变量配置示例
# ========================================
# 请将此文件复制为 .env 并填写实际值
# ========================================

# ========================================
# 授权配置（必需）
# ========================================

# 授权码（从供应商获取）
# 格式：CCUT-XXXXXX-XXXXXXXXXXXX-XXXX
# 示例：CCUT-202511-6DTZR3XMUKND-E102
LICENSE_KEY=your-license-key-here

# 加密密钥（必需）
# 用于加密存储 API 密钥，必须是 64 个十六进制字符（32 字节）
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-64-char-hex-encryption-key-here

# ========================================
# 数据库配置
# ========================================

# SQLite 数据库文件路径
DATABASE_URL=file:/data/db.sqlite

# ========================================
# 目录配置
# ========================================

# 临时文件目录（用于下载和处理中间文件）
TEMP_DIR=/temp

# 输出文件目录（最终成片存储位置）
OUTPUT_DIR=/output

# 日志文件目录
LOGS_DIR=/logs

# ========================================
# NCA 服务配置
# ========================================

# NCA API 请求超时时间（毫秒）
# 默认 5 分钟，大文件处理可能需要更长时间
NCA_REQUEST_TIMEOUT_MS=300000

# NCA Webhook 回调地址（必填，但目前未使用）
NCA_WEBHOOK_URL=https://www.google.com

# ========================================
# 系统配置
# ========================================

# Node.js 运行环境
NODE_ENV=production

# 日志级别: debug | info | warn | error
LOG_LEVEL=info

# ========================================
# API 密钥配置（通过 Web 界面设置，不在此文件配置）
# ========================================
# 以下密钥通过 /settings 页面设置，加密存储在数据库中：
#
# 1. Google Gemini (二选一)
#    - Vertex AI 模式（企业级）:
#      * Service Account JSON
#      * Project ID
#      * Location (如 us-central1)
#      * Model ID (如 gemini-2.0-flash-exp)
#
#    - AI Studio 模式（个人用户）:
#      * API Key (从 aistudio.google.com 获取)
#      * Model ID (如 gemini-2.0-flash-exp)
#
# 2. NCA Toolkit（视频处理）
#    - API Key
#
# 3. Fish Audio（语音合成）
#    - API Key
#    - Voice ID
#
# 4. Google Cloud Storage（可选，用于存储最终成片）
#    - Service Account JSON
#    - Bucket Name
